<% chats.each do |c| %>
  <% if c.user_id == current_user.id %>
    <div class="cu-message-box"><%= c.message %></div>
  <% elsif c.user_id == user.id %>
    <div class="u-message-box"><%= c.message %></div>
  <% end %>
<% end %>