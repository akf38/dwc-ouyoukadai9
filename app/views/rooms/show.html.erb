<div class="chatroom">
  <h2><%= @user.name %>さんとのチャットルーム</h2>

  <div class="chatlist-container">
    <%= render 'chatlist', chats: @chats, user: @user %>
  </div>
  
  <div class="chatform-container">
    <%= render 'chatform', chat: @chat, room: @room, user: @user %>
  </div>
</div>